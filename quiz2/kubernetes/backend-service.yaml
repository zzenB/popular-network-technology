apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: pdf-compressor-backend
  ports:
    - protocol: TCP
      port: 1337
      targetPort: 1337
  type: ClusterIP
